<Window x:Class="Aplikacja.EdytorTreningu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EdytorTreningu" Height="300" Width="330" Closed="Window_Closed">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Content="Ćwiczenie: " Grid.Row="0"/>
        <ComboBox Grid.Column="1" SelectedIndex="0" x:Name="cwiczenieCombo" Grid.Row="0" ItemsSource="{Binding}"/>

        <Label Content="Czas: " Grid.Row="1"/>
        <TextBox x:Name="czasTextbox" Grid.Row="1" Grid.Column="1" />

        <Button Content="Zapisz trening na dany dzień" x:Name="zapiszButton" Grid.Row="2" Click="zapiszButton_Click" />

    </Grid>
</Window>
